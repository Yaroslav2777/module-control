# Code smells та проблеми

1. Використання `var` замість `const` / `let`.
2. Вкладені `if`-вирази ускладнюють сприйняття.
3. Невизначене рівність `== true` замість строгого порівняння.
4. Погані назви: `process`, `d`, `r` не описують призначення.
5. Ручний цикл замість виразних методів масиву.
