# Завдання 3 — Тестування (Jest)

Покрито unit-тести для функції `calculateDiscount`.

## Файли
- `discount.js` — реалізація функції
- `discount.test.js` — тести на Jest
- `package.json` — конфігурація Jest

## Запуск
1. `npm install`
2. `npm test`
